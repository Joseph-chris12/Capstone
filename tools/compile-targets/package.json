{
  "name": "ar-gallery-compile-targets",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Compiles artwork photos into a MindAR .mind bundle and publishes it to Supabase.",
  "scripts": {
    "compile": "node compile.js",
    "dry-run": "node compile.js --dry-run",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.58.0",
    "playwright": "^1.56.0"
  }
}
